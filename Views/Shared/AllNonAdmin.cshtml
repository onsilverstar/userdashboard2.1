@model List<User>
<div class="container">
</div>
<h3>All Users</h3>
<div class="tablewrapper">
<table class="table">
    <thead>
        <tr>
            <td><strong>#<strong></strong></td>
            <td><strong>Name<strong></td>
            <td><strong>Email<strong></td>
            <td><strong>Created_At<strong></td>
            <td><strong>User_Level</strong></td>
        </tr>
    </thead>
    <tbody>
        @{
            int counter=0;
            foreach(var k in Model)
            {
                counter++;
                <tr>
                <td>@counter</td>
                <td><a asp-route-id="@k.Id" asp-controller="Home" asp-action="ViewUser">@k.FirstName @k.LastName</a></td>
                <td>@k.Email</td>
                <td>@k.CreatedAt</td>
                <td>@{foreach(var m in k.role){<p>@m</p>}}</td>
                </tr>
            }
        }

    </tbody>
</table>
</div>
</div>